# Argus 專題 — 工作交接（2026-06-09）

**承接：** [`docs/handoff-2026-06-07.md`](handoff-2026-06-07.md)（四階段原始計畫）
**用途：** 新對話開場白 / 交接給下一位 agent

---

## 本次（2026-06-09）已完成

**Phase 4「Nessus 差距補齊—被動 scanner」第一優先項目全部完成並上線：**

| 項目 | 狀態 |
|---|---|
| SSL/TLS 深度 scanner（憑證到期/弱 cipher/過期協議/自簽） | ✅ `backend/apps/scans/security/ssl_scanner.py` |
| Cookie 安全旗標（Secure/HttpOnly/SameSite） | ✅ `cookie_scanner.py` |
| 資訊洩露標頭 + CORS + CSP 品質 | ✅ `header_scanner.py` |
| OWASP Top 10 + CWE 對映（含既有 finding 關鍵字回填） | ✅ `owasp_mapper.py`（tag + backfill）|
| Finding model 加 `owasp_category` / `cwe_id` | ✅ migration `0009` |
| 整合進 tasks.py（Nuclei 後、silent-fail、純加法） | ✅ |
| 報告層呈現（serializer + Word 報告） | ✅ |
| DB 連線耗盡修正（CONN_MAX_AGE=0 + max_connections=200） | ✅ |

**驗證：** 單元測試 + 真實 Docker/Chrome E2E（靶機 `camb.xn--gst.tw`、`htb.xn--gst.tw`，後者抓到 `header-cors-wildcard`）；全套件 287 tests 綠；已 merge 遠端 22 commit 並 push、Docker 重建為最新版。

詳見 log：`2026-06-07_passive-security-scanners.md`、`2026-06-09_passive-security-e2e-verification.md`、`2026-06-09_db-conn-exhaustion-fix.md`、`2026-06-09_merge-push-identity-docker.md`。

---

## 下一部分（尚未開始）

依原始 roadmap，剩下的是**最大亮點但最高風險的 Phase 1–3「AI 自主攻擊鏈」**，以及 Phase 4 的次要 gap 項目。

### Phase 1–3：AI 攻擊鏈（導師加分重點）
- **Phase 1 環境**：docker-compose 加 Kali container；Mint 機架 DVWA / Metasploitable 2 靶機；確認內網互通。
- **Phase 2 Agent 工具**：`backend/apps/scans/security/kali_tools.py`（**仍 待建**），讓 Hermes-Agent 透過 `docker exec argus-kali-1` 呼叫 sqlmap / metasploit；開啟 `ARGUS_AGENT_ENABLED`（目前 `false`）。
- **Phase 3 CVE demo**：建議**先用 DVWA SQLi（sqlmap 一行出結果）跑通並錄影**當主 demo，Log4Shell 列 stretch goal（設定量大）。
- 規則見 [`backend/apps/scans/security/CLAUDE.md`](../backend/apps/scans/security/CLAUDE.md)「Kali Tools 設計原則」：只在 `ARGUS_AGENT_ENABLED=true AND scan_mode=active AND active_testing_authorized=True` 才可呼叫。

### Phase 4 次要 gap（視時間）
- SRI 缺失偵測（半天，HTML parser 已有基礎）→ 可加進 `header_scanner.py` 或新檔。
- DNS/郵件安全 SPF/DKIM/DMARC（`uv add dnspython`）。
- 第三方 JS 庫 CVE 對比（Retire.js / OSS Index，3–5 天）。
- 詳見 [`docs/nessus-gap-analysis.md`](nessus-gap-analysis.md) 第二/三優先。

---

## 現況快照 / 地雷（接手必讀）

- **git 身分**：本 repo `user.*` 已設為 `SmallLoOwO`；新 commit 為純 SmallLoOwO 作者。已推的歷史中，2026-06-09 前的 commit 作者為 `ntub`（+SmallLoOwO co-author），**刻意不改寫**（共用線上 repo，禁 force push）。
- **共用 repo + 公網上線**：`origin` 與部署機共用、`https://xn--gst.tw/` 線上服務中。push 前必讀 [`.claude/skills/argus-git-safety`](../.claude/skills/argus-git-safety)：只 stage 明確檔、掃機密、取得明確同意才推、不 force push。
- **DB max_connections=200**：由 `ALTER SYSTEM` 設定、存 db volume，**未進版本控制**；`docker compose down -v` 會失效需重設。
- **Docker**：`localhost:8080` 已是合併後最新版（web/worker/frontend 已重建）。整合測試一律用 Docker，禁本機 runserver。
- **測試殘留**：dev DB 有 `argus_e2e` 帳號與 scan 20/21/22，可清。
- **docker-compose**：web `DJANGO_DEBUG=false`（公網），故 `/django-admin/` 樣式會失效（需 --insecure 或 gunicorn）。
- **Kali 不要裝 Nuclei/Katana**（worker 已有，職責分離）；攻擊工具與 Nuclei 不可同時打同一目標。

---

## 相關文件快速索引

| 需要了解 | 先讀 |
|---|---|
| 四階段原始計畫、攻擊鏈架構 | `docs/capstone-roadmap.md` |
| Nessus 差距與優先順序 | `docs/nessus-gap-analysis.md` |
| 深度資安 sub-package 規則 | `backend/apps/scans/security/CLAUDE.md` |
| 掃描狀態機、Playwright | `backend/apps/scans/CLAUDE.md` |
| git/協作/部署安全 | `.claude/skills/argus-git-safety` |
| 本次設計 spec / 實作計畫 | `docs/superpowers/specs/2026-06-07-passive-security-scanners-design.md`、`docs/superpowers/plans/2026-06-07-passive-security-scanners.md` |
